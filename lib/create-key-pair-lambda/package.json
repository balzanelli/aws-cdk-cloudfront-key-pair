{
  "name": "create-key-pair-lambda",
  "private": true,
  "version": "0.1.0",
  "description": "AWS CDK custom resource to create SSH key pair",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.ts --outdir=dist --bundle --minify --sourcemap --platform=node --target=node16 --external:aws-sdk"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.114",
    "aws-lambda": "^1.0.7",
    "esbuild": "^0.17.15"
  },
  "dependencies": {
    "aws-sdk": "^2.1352.0"
  }
}
